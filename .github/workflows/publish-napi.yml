name: Publish Core npm package

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize]
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g. 1.0.0)'
        required: true

jobs:
  resolve-version:
    name: Resolve version
    if: github.event_name != 'release' || startsWith(github.event.release.tag_name, 'release-playground-core-v')
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      npm-tag: ${{ steps.version.outputs.npm-tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Determine version
        id: version
        shell: bash
        run: |
          CARGO_VERSION=$(grep '^version' crates/playground-napi/Cargo.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          SHORT_SHA=$(git rev-parse --short HEAD)
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            PR_NUM=${{ github.event.pull_request.number }}
            SHORT_SHA=$(echo "${{ github.event.pull_request.head.sha }}" | cut -c1-7)
            echo "version=${CARGO_VERSION}-pr-${PR_NUM}.${SHORT_SHA}" >> "$GITHUB_OUTPUT"
            echo "npm-tag=pr-${PR_NUM}" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event_name }}" = "release" ]; then
            RELEASE_VERSION=$(echo "${{ github.event.release.tag_name }}" | sed 's/^.*-v//')
            echo "version=${RELEASE_VERSION}" >> "$GITHUB_OUTPUT"
            echo "npm-tag=latest" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event_name }}" = "push" ]; then
            echo "version=${CARGO_VERSION}-beta.${SHORT_SHA}" >> "$GITHUB_OUTPUT"
            echo "npm-tag=beta" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ inputs.version }}" >> "$GITHUB_OUTPUT"
            echo "npm-tag=latest" >> "$GITHUB_OUTPUT"
          fi

      - name: Print version
        run: |
          echo "Version: ${{ steps.version.outputs.version }}"
          echo "NPM tag: ${{ steps.version.outputs.npm-tag }}"

  build:
    name: Build ${{ matrix.npm_pkg }}
    needs: resolve-version
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-latest
            npm_pkg: release-playground-core-linux-x64-gnu
          - target: aarch64-unknown-linux-gnu
            runner: ubuntu-latest
            npm_pkg: release-playground-core-linux-arm64-gnu
            cross: true
          - target: x86_64-apple-darwin
            runner: macos-latest
            npm_pkg: release-playground-core-darwin-x64
          - target: aarch64-apple-darwin
            runner: macos-14
            npm_pkg: release-playground-core-darwin-arm64
          - target: x86_64-pc-windows-msvc
            runner: windows-latest
            npm_pkg: release-playground-core-win32-x64-msvc
          - target: aarch64-pc-windows-msvc
            runner: windows-latest
            npm_pkg: release-playground-core-win32-arm64-msvc
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross
        if: matrix.cross
        run: cargo install cross --locked

      - name: Build native module
        shell: bash
        run: |
          if [ "${{ matrix.cross }}" = "true" ]; then
            cross build --release --target ${{ matrix.target }} -p release-playground-napi
          else
            cargo build --release --target ${{ matrix.target }} -p release-playground-napi
          fi

      - name: Copy .node file into npm package
        shell: bash
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            cp target/${{ matrix.target }}/release/release_playground_napi.dll crates/playground-napi/npm/${{ matrix.npm_pkg }}/release-playground-core.node
          elif [ "${{ runner.os }}" = "macOS" ]; then
            cp target/${{ matrix.target }}/release/librelease_playground_napi.dylib crates/playground-napi/npm/${{ matrix.npm_pkg }}/release-playground-core.node
          else
            cp target/${{ matrix.target }}/release/librelease_playground_napi.so crates/playground-napi/npm/${{ matrix.npm_pkg }}/release-playground-core.node
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Set npm version
        working-directory: crates/playground-napi/npm/${{ matrix.npm_pkg }}
        shell: bash
        run: npm version --no-git-tag-version --allow-same-version "${{ needs.resolve-version.outputs.version }}"

      - name: Publish platform package
        working-directory: crates/playground-napi/npm/${{ matrix.npm_pkg }}
        run: npm publish --tag ${{ needs.resolve-version.outputs.npm-tag }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  publish-meta:
    name: Publish @jpwesselink/release-playground-core
    needs: [resolve-version, build]
    if: always() && needs.resolve-version.result == 'success' && needs.build.result != 'cancelled'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Set version
        working-directory: crates/playground-napi
        shell: bash
        run: |
          VERSION="${{ needs.resolve-version.outputs.version }}"
          node -e "
            const pkg = require('./package.json');
            pkg.version = '$VERSION';
            for (const dep of Object.keys(pkg.optionalDependencies || {})) {
              pkg.optionalDependencies[dep] = '$VERSION';
            }
            require('fs').writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

      - name: Publish meta-package
        working-directory: crates/playground-napi
        run: npm publish --tag ${{ needs.resolve-version.outputs.npm-tag }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
